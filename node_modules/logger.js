
const EventEmitter = require("events")

const emmiter = new EventEmitter()

var url = "www.google.com"

var log = function (message) {
	console.log(message)
	// Raise event 2
	emmiter.emit("logging",{"message":"Hi"})
}

module.exports.log = log